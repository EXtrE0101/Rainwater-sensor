# Rainwater-sensor
🌧️ Rain Water Detection System with Servo Motor
📌 Project Overview

This project detects rain using a rain water sensor and automatically moves a servo motor when rain is detected. It is built using an Arduino UNO.

When rainwater touches the sensor plate:

The sensor sends a signal to Arduino.

The Arduino rotates the servo motor.

When dry, the servo returns to its original po
